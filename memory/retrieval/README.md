# Retrieval / External Memory

Reference material meant to be keyword-searched (`kb.py search`) rather
than read in full each time — the file-based stand-in for a vector store.
Keep raw sources here unmodified; synthesis belongs in `semantic/`.

Scaffold a new entry: `../../scripts/kb.py new --type retrieval "<name>"`
