# Prospective Memory

Future intentions and scheduled goals not yet executed — things to do or
check later. Use the `due:` frontmatter field. When a prospective entry
fires, act on it and move the outcome to `episodic/`.

Scaffold a new entry: `../../scripts/kb.py new --type prospective "<name>"`
