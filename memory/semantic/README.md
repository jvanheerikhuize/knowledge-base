# Semantic Memory

Durable facts, preferences, and domain knowledge — true independent of when
they were learned. Example: "the user prefers terse commit messages",
"the staging DB is Postgres 15".

Scaffold a new entry: `../../scripts/kb.py new --type semantic "<name>"`
